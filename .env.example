# Example environment variables for the Vite React project
# Copy this file to .env (or .env.local) and update values as needed.

# Vite uses VITE_ prefix for client-exposed env vars
VITE_API_BASE_URL=https://api.example.com
VITE_APP_TITLE=Prakriti Seva - The Eco Dharmik Platform

# Example: enable/disable feature flags
VITE_ENABLE_SOME_FEATURE=false

# Local dev values
# VITE_API_BASE_URL=http://localhost:3000

# Notes:
# - Create a `.env` or `.env.local` file and add project-specific secrets. Do NOT commit `.env` to git.
# - On Windows PowerShell, you can set an env variable for the current session with:
#   $env:VITE_API_BASE_URL = 'http://localhost:3000'
# - When running `npm run dev`, Vite will load `.env` and `.env.development` files automatically.
